# Repository Guidelines

All agent guidance for this repository lives in **`CLAUDE.md`** (commands, architecture pointers,
tool/engine playbooks) and **`docs/code-map.json`** (generated slug → files/engine/group map).
System-level patterns are in `ARCHITECTURE.md`. Do not duplicate them here.
