---
import NetworkWidget from '@tools/_shared/NetworkWidget.astro';
import { config } from './config';
---

<NetworkWidget slug={config.slug} networkId="cidr" config={config} emptyText="Enter an IPv4 CIDR to calculate." />
