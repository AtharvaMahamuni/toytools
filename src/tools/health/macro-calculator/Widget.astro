---
import WellnessWidget from '@tools/_shared/WellnessWidget.astro';
import { config } from './config';
---

<WellnessWidget slug={config.slug} wellnessId={config.processorId!} config={config} emptyText="Enter your daily calories and pick a diet style to see your protein, carb, and fat targets." />
