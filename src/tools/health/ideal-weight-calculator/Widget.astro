---
import WellnessWidget from '@tools/_shared/WellnessWidget.astro';
import { config } from './config';
---

<WellnessWidget slug={config.slug} wellnessId={config.processorId!} config={config} emptyText="Enter your height and sex to see ideal-weight estimates and the healthy weight range for your height." />
