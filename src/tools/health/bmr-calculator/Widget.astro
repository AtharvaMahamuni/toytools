---
import WellnessWidget from '@tools/_shared/WellnessWidget.astro';
import { config } from './config';
---

<WellnessWidget slug={config.slug} wellnessId={config.processorId!} config={config} emptyText="Enter your sex, age, height, and weight to estimate the calories your body burns at complete rest." />
