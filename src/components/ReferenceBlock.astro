---
interface Props {
  type: 'key-idea' | 'common-mistake' | 'note';
  heading?: string;
}

const defaultHeadings: Record<Props['type'], string> = {
  'key-idea': 'Key Idea',
  'common-mistake': 'Common Mistake',
  'note': 'Note',
};

const { type, heading } = Astro.props;
const label = heading ?? defaultHeadings[type];
---

<aside class="ref-block" data-type={type}>
  <p class="ref-heading">{label}</p>
  <slot />
</aside>

<style>
  .ref-block {
    padding: var(--space-4) var(--space-5);
    border-left: 3px solid var(--color-border-strong);
    border-radius: 0 var(--radius-md) var(--radius-md) 0;
    background: var(--color-bg-tertiary);
    margin-block: var(--space-4);
  }

  .ref-block[data-type="key-idea"] {
    border-left-color: var(--color-accent);
    background: var(--color-accent-subtle);
  }

  .ref-block[data-type="common-mistake"] {
    border-left-color: var(--color-gold);
    background: var(--color-gold-subtle);
  }

  .ref-block[data-type="note"] {
    border-left-color: var(--color-border-strong);
    background: var(--color-bg-tertiary);
  }

  .ref-heading {
    font-weight: var(--font-weight-semibold);
    font-size: var(--text-sm);
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--color-text-muted);
    margin-bottom: var(--space-2);
    max-width: unset;
  }

  .ref-block[data-type="key-idea"] .ref-heading {
    color: var(--color-accent);
  }

  .ref-block[data-type="common-mistake"] .ref-heading {
    /* Text, not decoration: plain --color-gold is 4.31:1 on this block's tint, just under AA.
       Same reason --color-accent-strong exists for the accent. */
    color: var(--color-gold-strong);
  }

  :global(.ref-block p:not(.ref-heading)) {
    font-size: var(--text-base);
    max-width: unset;
    color: var(--color-text);
  }
</style>
